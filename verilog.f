// =========================================================
// 1. Include Directories (-I)
//    `include "file.svh" 등을 찾기 위한 경로 설정
// =========================================================
-Irtl
-Irtl/pkg
-Irtl/stages
-Irtl/units
-Irtl/memories
-Irtl/programs

// =========================================================
// 2. Packages (최상단 배치)
//    다른 모듈이 참조하는 패키지 파일은 가장 먼저 컴파일되어야 합니다.
// =========================================================
rtl/pkg/riscv_pkg.sv

// =========================================================
// 3. Automatic Module Search (-y)
//    나머지 일반 모듈들(fetch, decode, alu 등)은 폴더에서 자동으로 찾습니다.
// =========================================================
-y rtl
-y rtl/stages
-y rtl/units
-y rtl/memories
-y rtl/programs